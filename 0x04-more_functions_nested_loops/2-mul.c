#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * Return: multiplication result of both integers
 * @a: first integer
 * @b: second integer
 */

int mul(int a, int b)
{
	return (a * b);
}
