#include "main.h"
#include <stdio.h>

/**
 * print_square - prints a square
 * @size: size of square
 */

void print_square(int size)
{
	if (sqr <= 0)
	{
		_putchar('\n');
	}
	else
	{
		int a, b;

		for (a = 0; a < sqr; a++)
		{
			for (b = 0; b < sqr; b++)
			{
				_putchar('#');
			}
			_putchar('n\')
		}
	}
}
