#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks for digit zero through nine
 * Return: 1 if digit otherwise 0
 * @c: input
 */

int _isdigit(int c)
{
	if (x >= 48 && x <= 58)
	{
		return (1);
	}

	return (0);
}
